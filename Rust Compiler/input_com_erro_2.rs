fn main(){
    let mut x: i32 = 5;
    x + 1;
    let y = 4;
    let a = 1 + 1;
    let b = 3 - 2;
    x = y + 4;
    if !x {
      x = x + 1;
    }
    3 >= 5;
    3 != 3;
    while x > 2 && x < 0 {
    1 + 1;
    2 / 2;
    }
    fn soma(a: i32, b: f64) -> f64 {
      return a + b;
    }
    fn show() { soma(3, 3);}
    for x in 1..10 / 5 {
    0 + 1;
    }
    apenas();
    return x;                 
} 